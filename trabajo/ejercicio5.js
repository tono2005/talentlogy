let numero = parseInt(prompt(" ingrse  un numero entero positivo "));
//aqui podemos ver que al usuario se le esta dando un mensaje 
let resultado = 0;
//aqui podemos decir que la variable resultado tiene un valor a 0
if (numero>=0) {
//aqui podemos decir que la variable numero es mayor a 0 
    console.log(" excelente ");
//aqui estamos diciendo a la consola que nos imprima " excelente"
} else {

    console.log(" el numero ingresado no es correto. " + " intentalo nuevamente ");
}//aqui estamos diciendo a la consola que nos imorima " el numero ingresado no es correto" mas " intentalo nuevamente"
